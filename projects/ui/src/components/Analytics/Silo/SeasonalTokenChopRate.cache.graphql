query CachedSeasonalTokenChopRate($where: String) {
  seasons: cache_unripeTokenHourlySnapshots(
    where: $where
    orderBy: "season"
    orderDirection: "desc"
  ) {
    id
    season
    createdAt
    chopRate
    deltaChopRate
    choppableBdvOne
  }
}
