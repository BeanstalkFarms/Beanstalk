{
  "name": "beanft-indexer",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ beanft-indexer",
    "create-local": "graph create --node http://localhost:8020/ beanft-indexer",
    "remove-local": "graph remove --node http://localhost:8020/ beanft-indexer",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 beanft-indexer",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.46.1",
    "@graphprotocol/graph-ts": "^0.29.1",
    "ethers": "^6.3.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
