query CachedSeasonalPods($where: String) {
  seasons: cache_fieldHourlySnapshots(
    where: $where
    orderBy: "season"
    orderDirection: "desc"
  ) {
    id
    season
    createdAt
    unharvestablePods
  }
}
