query CachedSeedsPerSeason($where: String) {
  seasons: cache_whitelistTokenHourlySnapshots(
    where: $where
    orderBy: "season"
    orderDirection: "desc"
  ) {
    id
    season
    createdAt
    stalkEarnedPerSeason
  }
}
