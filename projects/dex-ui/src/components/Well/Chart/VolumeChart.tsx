import React from "react";
import { FC } from "src/types";
import { ChartContainer } from "./ChartStyles";

type Props = {};
export const VolumeChart: FC<Props> = ({}) => {
  return <ChartContainer>VolumeChart</ChartContainer>;
};
